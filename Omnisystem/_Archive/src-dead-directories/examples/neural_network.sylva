// SYLVA - Train Neural Network

tensor data = randn([1000, 784])
let model = model {
    layer1: dense(784, 128),
    layer2: dense(128, 64),
    output: dense(64, 10),
}

let loss = train(model, data)
let predictions = predict(model, data)
